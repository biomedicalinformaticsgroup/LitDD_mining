# Pinned to the development versions used in the manuscript (see README "Software").
# Series pins (==X.Y.*) reflect the tested minor versions and avoid the unbounded
# resolution that lower-bound (>=) pins allow for the tightly-coupled CUDA/torch/vLLM stack.
# For a fully hash-locked file, run `pip-compile --generate-hashes` in the dev environment.
#
# NOTE: install torch separately against your CUDA build first, e.g.
#   pip install torch==2.8.* --index-url https://download.pytorch.org/whl/cu129
#
# transformers >= 4.48 is REQUIRED, not optional: the released LitDD screen is a
# ModernBERT architecture (`ModernBertForSequenceClassification`, base
# `thomas-sounack/BioClinical-ModernBERT-large`), and ModernBERT support landed in
# transformers 4.48. Earlier versions fail to load it with `KeyError: 'modernbert'`.
# torch/vLLM are pinned to the pair the deployment images actually ship
# (pytorch/pytorch:2.8.0-cuda12.9 and vllm/vllm-openai:v0.10.1.1); torch 2.4 does
# not satisfy vllm 0.10.x.

# Core ML / NLP
torch==2.8.*
transformers==4.48.3
datasets==2.20.*
sentence-transformers==3.0.*
evaluate==0.4.*
vllm==0.10.1.1

# Data handling
pandas==2.2.*
numpy==1.26.*
polars==1.5.*
pyarrow==17.*
scikit-learn==1.5.*

# PubMed / literature ingestion
pubmed_parser==0.5.*
lxml==5.2.*
requests==2.32.*

# Ontology / graph
networkx==3.3.*
node2vec==0.4.6
owlready2==0.46

# HPO annotation
FastHPOCR==1.0.*

# Visualisation (GPU UMAP/HDBSCAN via RAPIDS optional)
umap-learn==0.5.*
datamapplot==0.4.*
matplotlib==3.8.*

# Utilities
tqdm==4.66.*
